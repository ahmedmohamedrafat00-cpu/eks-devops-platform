apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-source-pvc
  namespace: ci
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: gp3
